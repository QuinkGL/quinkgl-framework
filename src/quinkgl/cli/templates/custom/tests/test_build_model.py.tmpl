# Conforms to SWARM_ARCHITECTURE_SPEC.md v2.0.0
"""Tests for build_model in {project_name}."""
import pytest

from peer_script import build_model


def test_build_model_raises_not_implemented():
    with pytest.raises(NotImplementedError):
        build_model(input_shape={input_shape}, output_shape={output_shape})
