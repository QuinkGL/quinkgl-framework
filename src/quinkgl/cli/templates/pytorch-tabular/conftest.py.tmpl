# Conforms to SWARM_ARCHITECTURE_SPEC.md v2.0.0
"""
Pytest fixtures for {project_name}.
Generated by QuinkGL v{quinkgl_version} ({template} template).
"""

import pytest_asyncio

from quinkgl.testing import local_swarm_fixture


@pytest_asyncio.fixture
async def fake_swarm():
    async with local_swarm_fixture() as sw:
        yield sw
